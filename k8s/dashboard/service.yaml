apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: vigilantai
  labels:
    app.kubernetes.io/name: dashboard
    app.kubernetes.io/part-of: vigilantai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dashboard
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
