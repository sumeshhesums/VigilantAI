apiVersion: v1
kind: Service
metadata:
  name: camera-gateway
  namespace: vigilantai
  labels:
    app.kubernetes.io/name: camera-gateway
    app.kubernetes.io/part-of: vigilantai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: camera-gateway
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
